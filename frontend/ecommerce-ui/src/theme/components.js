const components = {
  MuiButton: {
    styleOverrides: {
      root: {
        borderRadius: 12,
        padding: "12px 28px",
        fontWeight: 600,
      },
    },
  },

  MuiCard: {
    styleOverrides: {
      root: {
        borderRadius: 20,
        boxShadow: "0 8px 30px rgba(0,0,0,0.08)",
      },
    },
  },

  MuiTextField: {
    styleOverrides: {
      root: {
        borderRadius: 12,
      },
    },
  },
};

export default components;